upstream backend_servers {
    # server 地址 [权重]
    server 8.153.15.232:6666 weight=1;
    server 47.123.2.199:6666     weight=2; 

    server 8.153.15.232:6666 weight=1 max_fails=2 fail_timeout=30s;
}

server {
    listen       80;
    listen  [::]:80;
    server_name  localhost;

    # 开启静态文件缓存
    open_file_cache max=1000 inactive=20s;
    open_file_cache_valid 30s;
    open_file_cache_min_uses 2;
    open_file_cache_errors on;

    root   /usr/share/nginx/html;
    index  index.html index.htm;

    # 拦截常见的扫描路径
    location ~* ^/(config|init|ping|home|events|v1/config|im/v2|common/menus) {
        return 444;
    }

    # 拦截 .php, .env, .git 等敏感文件后缀
    location ~* \.(php|env|bak|config|sql|git|sh)$ {
        return 444;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }


    # 媒体文件缓存设置（视频、音频）
    location ~* \.(mp4|webm|ogg|mp3|wav)$ {
        expires 30d;
        add_header Cache-Control "public, max-age=2592000";
        access_log off;
    }
    
    # 图片文件缓存设置
    location ~* \.(jpg|jpeg|png|gif|ico|svg|webp)$ {
        expires 7d;
        add_header Cache-Control "public, max-age=604800";
        access_log off;
    }
    
    # CSS、JavaScript文件缓存设置
    location ~* \.(css|js)$ {
        expires 7d;
        add_header Cache-Control "public, max-age=604800";
    }
    
    # 字体文件缓存设置
    location ~* \.(woff|woff2|ttf|otf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, max-age=2592000";
        access_log off;
    }

    location /api/ {
        proxy_pass http://backend_servers/;   # 后端服务地址
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Internal-Secret "fdcsky";

        # SSE (Server-Sent Events) 配置
        proxy_buffering off;
        proxy_cache off;
        proxy_set_header Connection '';
        proxy_http_version 1.1;
        chunked_transfer_encoding on;
        proxy_read_timeout 86400s; # 长连接超时时间
    }

    
}
